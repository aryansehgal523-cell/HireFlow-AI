{
  "buildCommand": "npm run build -w apps/web && npx prisma migrate deploy --schema=apps/web/prisma/schema.prisma",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/jobs/sync",
      "schedule": "0 0 * * *"
    }
  ]
}
