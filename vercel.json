{
  "buildCommand": "npx prisma db push --schema=apps/web/prisma/schema.prisma --accept-data-loss && npm run build -w apps/web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/jobs/sync",
      "schedule": "0 0 * * *"
    }
  ]
}
